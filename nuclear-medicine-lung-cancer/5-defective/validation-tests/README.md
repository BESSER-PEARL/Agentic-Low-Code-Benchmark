# Acceptance Tests

This directory contains the acceptance tests for the application.

The tests are derived from the natural-language requirements and describe the expected observable behavior of the application. They are expressed using the Gherkin/Behave approach and are used to evaluate whether the generated application conforms to the specified requirements.

The acceptance tests are independent of the development approach and are applied consistently to the applications produced by the different approaches evaluated in the benchmark.